import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth}from'../lib/auth.js';import*as S from'../styles.js';
export default function History(){const[c,setC]=useState([]);useEffect(()=>{requireAuth(window.location.href);supabase.from('ai_conversations').select('*').order('created_at',{ascending:false}).then(({data})=>setC(data||[]));  },[]);
return React.createElement('div',{style:S.page},React.createElement('h1',{style:S.h1},'Conversation History'),c.length===0&&React.createElement('div',{style:S.card},React.createElement('p',{style:S.muted},'No conversations yet.')),c.map(x=>React.createElement('div',{key:x.id,style:{...S.card,cursor:'pointer'},onClick:()=>window.location.href='/'},React.createElement('h2',{style:S.h2},x.title||'Untitled'),React.createElement('p',{style:S.muted},new Date(x.created_at).toLocaleString(),' · '+x.model))));}